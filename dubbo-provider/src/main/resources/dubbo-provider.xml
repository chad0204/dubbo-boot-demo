<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
   http://www.springframework.org/schema/beans/spring-beans.xsd
   http://code.alibabatech.com/schema/dubbo
   http://code.alibabatech.com/schema/dubbo/dubbo.xsd
   http://dubbo.apache.org/schema/dubbo
   http://dubbo.apache.org/schema/dubbo/dubbo.xsd">

    <!--过气配置-->
    <!--<dubbo:application name="dubbo-provider"/>-->
    <!--指定当前应用/服务名
    <dubbo:application name="dubbo-provider"/>

    指定注册中心
    <dubbo:registry address="zookeeper://127.0.0.1:2181" />
    <dubbo:registry protocol="zookeeper" address="47.98.213.18:2181"/>

     用dubbo协议在20880端口暴露服务
    <dubbo:protocol name="dubbo" port="20880" />

    暴露服务接口，ref指定实现的bean
    <dubbo:service interface="com.pc.dubboapi.service.UserService" ref="userServiceImpl"/>
    <bean id="userServiceImpl" class="com.pc.dubboprovider.service.impl.UseServiceImpl"/>

    连接控中心，配置协议，自动从注册中心发现
    <dubbo:monitor protocol="register"/>
    <dubbo:monitor address="127.0.0.1:8282"/>-->





</beans>